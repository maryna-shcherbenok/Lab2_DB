@model IEnumerable<object>
@{
    ViewData["Title"] = "Результати запиту 11";
    var props = new[] { "TitlePH" };
}

<h2>Результати запиту 11</h2>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Видавництво</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.GetType().GetProperty("TitlePH")?.GetValue(item)</td>
            </tr>
        }
    </tbody>
</table>

<a class="btn btn-primary mt-3" asp-action="Index" asp-controller="Queries">Назад</a>
